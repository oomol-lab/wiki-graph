Help Type: command
Command: spinedigest estimate

Purpose:
  Estimate build cost, calls, tokens, time, and risk.

Usage:
  spinedigest estimate <archive.sdpub> [--stage <source|graph|summary|ready>] [--json]

Contract:
  - Mutates: no.
  - LLM: no.
  - JSON: yes.

Examples:
  spinedigest estimate book.sdpub --stage ready
  spinedigest estimate book.sdpub --stage graph --json
