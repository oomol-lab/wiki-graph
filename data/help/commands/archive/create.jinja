Help Type: command
Command: spinedigest create

Purpose:
  Create an LLM Wiki archive from source material at source stage.

Usage:
  spinedigest create <archive.sdpub> [source] [--input-format <format>] [--llm <json>] [--prompt <text>]

Contract:
  - Mutates: yes, writes the archive path.
  - LLM: no; create stops at source stage.
  - stdin: supported with --input-format markdown or txt.
  - JSON: no.

Examples:
  spinedigest create book.sdpub ./book.md --input-format markdown
  cat article.md | spinedigest create article.sdpub --input-format markdown
