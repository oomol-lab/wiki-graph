Help Type: command
Command: spinedigest grep

Purpose:
  Alias for deterministic archive text search.

Usage:
  spinedigest grep <archive.sdpub> <query> [--json]

Contract:
  - Mutates: no.
  - LLM: no.
  - JSON: yes.

Examples:
  spinedigest grep book.sdpub "Agent"
