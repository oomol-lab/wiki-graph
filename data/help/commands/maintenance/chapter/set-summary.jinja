Help Type: command
Command: wikigraph chapter set-summary

chapter set-summary

Purpose:
  Fill a graph-stage chapter with a manual final summary.

Usage:
  wikigraph chapter set-summary <path> --chapter <id> [--input <path>] [--llm <json>] [--help|-h]

Input:
  - If `--input` is omitted, summary text is read from stdin.
  - Summary text is stored as the chapter's final summary.
  - A common workflow is `read > /tmp/summary.md`, edit the file, then pass it back with `--input`.

Preconditions:
  - The chapter must be `graph`.
  - If the chapter is `source`, run:
    wikigraph queue add <path> --chapter <id> --task reading-graph --accept-cost

Behavior:
  - Moves the chapter to `summary`.
  - Does not generate or modify graph data.
  - Does not call an LLM provider.

See also:
  - wikigraph queue --help
  - wikigraph chapter status --help
  - wikigraph help command
