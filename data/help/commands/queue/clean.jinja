Help Type: command
Command: wikigraph queue clean

Purpose:
  Remove old terminal job records and retained files.

Usage:
  wikigraph queue clean

Behavior:
  - Succeeded jobs delete their workspace by default when they finish.
  - Failed and canceled jobs keep their workspace for debugging.
  - Clean removes retained terminal history according to queue retention rules.

Example:
  wikigraph queue clean
